{
  "interface_scale": {{ if eq .chezmoi.hostname "hex" }} 2.0 {{ else }} 1.25 {{ end }},
  "render_device_type": "{{ if eq .chezmoi.hostname "turnipseed" }}OptiX{{ else }}NONE{{ end }}",
  "theme": "Deep Grey"
}
